[railway]
  id = "etsy688-local"
  plugin = "nodejs"

[build]
  builder = "NIXPACKS"
  builderVersion = "1"

[deploy]
  numReplicas = 1
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10

[environment]
  PORT = "3001"
  NODE_ENV = "production"
